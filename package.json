{
  "name": "fourmorgue",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"pnpm --filter backend dev\" \"pnpm --filter frontend dev\"",
    "build": "pnpm --filter fourmorgue-frontend build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "@duneanalytics/client-sdk": "^0.3.5",
    "@google/genai": "^1.50.1",
    "dotenv": "^17.4.2"
  }
}